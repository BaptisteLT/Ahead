private function get{{ capitalizedattributeName }}(): ?\DateTimeImmutable{
    return $this->{{ attributeName }};
}

private function set{{ capitalizedattributeName }}(?\DateTimeImmutable ${{ attributeName }}): static{
    $this->{{ attributeName }} = ${{ attributeName }};

    return $this;
}